%h1 Listing users

%table
  %tr
    %th Name
    %th Mail
    %th Login
    %th Summary
    %th Description
    %th
    %th
    %th

  - @users.each do |user|
    %tr
      %td= link_to user.name, "/login/#{user.login}"
      %td= user.mail
      %td= user.login
      %td= user.summary
      %td= user.description
      %td= link_to 'Show', user
      %td= link_to 'Edit', edit_user_path(user)
      %td= link_to 'Destroy', user, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New User', new_user_path
